{
  "name": "kria-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static prototype for the Kria Hebrew fluency app",
  "scripts": {
    "start": "npm --prefix kria run dev",
    "dev": "npm --prefix kria run dev",
    "build": "npm --prefix kria run build",
    "serve": "npm --prefix kria run serve"
  }
}
